cloudfairy_module:
  type: component
  name: env-base
  internal: true
  vendor: cloudfairy
  displayName: "Environment Base Module"
  runtime: terraform
  dependencies:
    - "cloud_provider"
  entry:
    aws: base.aws.tf
    az: base.az.tf
    gcp: base.gcp.tf
    local: base.local.tf
  interface:
    - base
  properties: []
