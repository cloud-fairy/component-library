cloudfairy_module:
  name: eks
  displayName: "Managed K8S Cluster"
  type: component
  interface:
    - eks
  internal: false
  vendor: cloudfairy
  runtime: terraform
  entry:
    aws: aws
  dependencies:
    - "cloud_provider"
    - network
    - subnet
  properties: 
    - name: name
      type: string
      displayName: "Cluster Name"
      defaultValue: "eks_new"

    - name: k8s_version
      type: string
      displayName: Cluster Version
      defaultValue: "1.24"

    - name: enable_public_access
      type: bool
      displayName: enable_public_access
      defaultValue: true
      
    # - name: ssh_key_secret_arn
    #   type: string
    #   displayName: ssh_key_secret_arn
    #   defaultValue: ""
    # - name: cluster_additional_security_group_ids
    #   type: string
    #   displayName: additional security groups
    #   defaultValue: "[]"
