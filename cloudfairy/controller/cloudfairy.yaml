cloudfairy_module:
  name: controller
  displayName: "Auxiliary"
  type: component
  interface:
    - controller
  internal: true
  vendor: cloudfairy
  runtime: terraform
  entry:
    aws: aws
    local: controller.local.tf
  dependencies:
    - "cloud_provider"
    - cluster
    - base
  properties: []
